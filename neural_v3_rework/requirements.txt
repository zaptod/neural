# Neural Fights - Requirements
# =============================
# Simulador de combate 2D com IA avançada
# Python 3.10+ recomendado

# === CORE DEPENDENCIES ===

# Interface gráfica do simulador de combate
pygame>=2.5.0

# Interface moderna para o modo torneio
customtkinter>=5.2.0

# === VIDEO PIPELINE DEPENDENCIES ===
# Necessárias para gravar lutas em vídeo (video_pipeline/)
# Instalar com: pip install -r requirements.txt -r requirements-video.txt
#               ou: pip install -e ".[video]"  (via pyproject.toml)
opencv-python>=4.8.0
numpy>=1.24.0

# === DEVELOPMENT DEPENDENCIES ===
# Instalar com: pip install -e ".[dev]"  (via pyproject.toml)
pytest>=7.0.0        # E04 Sprint 11: movido de comentário para dependência ativa
pytest-cov>=4.0.0

# === OPTIONAL (análise de performance) ===
# line_profiler>=4.0.0
# memory_profiler>=0.60.0

# === NOTES ===
#
# Instalação rápida (jogo apenas):
#   pip install pygame customtkinter
#
# Instalação completa:
#   pip install -r requirements.txt
#
# Instalação como pacote (recomendado para dev):
#   pip install -e ".[dev,video]"
#
# Executar o jogo:
#   python run.py
#
# Executar modo torneio:
#   python run_tournament.py
